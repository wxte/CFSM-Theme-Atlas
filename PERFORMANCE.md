# v0.2.2 性能修复

基线报告：https://pagespeed.web.dev/analysis/https-dash-wxt-me/vzmijtddsk

2026-09-07 报告：桌面 FCP/LCP 均 0.4 秒，TBT 7,870 毫秒，主线程 32.8 秒；地球脚本产生大量长任务。手机 FCP 1.5 秒、主线程 31.8 秒，LCP/TBT 报 NO_LCP，未得到性能分数。这是基线，不是新版得分。

## 主题内修复

- COBE 接收 CSS 尺寸，仅让库应用一次像素倍率。桌面画布 1152 → 639，手机 597 → 397；CSS 显示尺寸不变。
- 地球进入可视区域再初始化，离屏、切换视图、隐藏标签页、关闭地球时停止绘制；待执行帧也检查可见性。保留自动旋转、拖动和减少动态效果设置。
- 缓存标签尺寸，只有标签文字或容器尺寸变化时重新测量，避免每帧反复读写布局。
- 预加载原生模块依赖，缩短串行发现链；COBE 仍按需加载。
- 表格每个直接子项具有 cell 语义，操作按钮保留按钮语义，展开详情进入独立单元格。row 容器改用 div。
- 深浅主题的小文字颜色在页面、卡片、浅底及选中底色上满足至少 4.5:1 对比度。

## 验证

13 项 Node 测试覆盖数据、路由、图表、标签布局、地球可见性调度和文字配色。DOM 集成检查覆盖真实 API 样本、增量更新、详情保留、筛选、断线重连。
本地浏览器在 1440 和 390 像素宽度确认 7 个节点、详情展开、无横向溢出及实际画布 639/397。真实 REST 请求成功，WebSocket 收到订阅确认及两批更新。

## 需上线复测的边界

- 不承诺新 Lighthouse 分数；发布并重新应用主题后分别重测手机与桌面，比较 TBT 与 NO_LCP 是否消失。
- 报告中的 robots.txt 返回主题 HTML，属于服务端路径回退，主题 CSS/JS 无法修复。本次未修改后台路由。
- 报告记录 WebSocket ERR_NAME_NOT_RESOLVED，是测试时的域名解析失败；需要线上复测确认，不通过屏蔽日志掩盖问题。主题保留轮询回退。
