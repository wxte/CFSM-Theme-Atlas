# WXT Atlas v0.2.8 验证记录

日期：2026-09-07。使用本地预览代理连接用户的 CFSM 原生公开接口，7 台真实节点。未修改后端或管理设置。

## v0.2.7 · Geist Pixel 与顶部操作收口

- 顶部移除齿轮与独立显示设置分页；日夜与动态效果由右上角图标直接切换。
- 资源卡位于 KPI 下方且不再重复显示分区标题。
- Geist Pixel 作为展示字体加载失败时自动回退 Geist Sans，不影响正文和数据可读性。

## v0.2.8 · 本地字体与小字号可读性

- 移除远程 Geist Pixel CSS；正文、中文、小数据和节点名称使用本地 Atlas UI（Sarasa UI SC 子集），品牌和 KPI 大数字使用本地 Atlas Pixel Display。
- 保留 `unicode-range` 字体分片与 `font-display: swap`；首屏只预加载常用字集和展示数字字体，不访问 CDN 字体域名。
- 浏览器报告确认正文、品牌和 KPI 的计算字体分别为 Atlas UI / Atlas Pixel Display；桌面和 390px 手机均无横向溢出，字体请求全部来自 `127.0.0.1` 主题资源。首页只请求 Atlas UI Regular、Atlas UI Semibold 和 Geist Pixel Display 三个 WOFF2，合计约 111 KiB；动态节点名使用系统中文回退。
- `fonts.test.js` 校验没有远程字体 URL、所有 CSS 引用的字体文件存在，并保留本地字体许可文件。

## 自动检查

### v0.2.6 · 顶部工具栏与概览资源

- 资源概况并入概览，主导航只保留概览、节点、网络；旧的 `#resources` 深链接仍回退到概览，避免已有书签失效。
- 顶部日 / 夜、动态效果、活动、设置均使用同源内联 SVG 图标；活动入口显示会话徽标，弹出最近五条记录并可进入完整活动视图。
- 活动在首次成功读取节点后写入一条连接记录，因此空白会话不会再显示“无记录”误导；后续上下线、指标异常和恢复继续实时记录。
- 浅色主题的实时上下行、网络曲线、节点在线率和资源进度条使用更高饱和度颜色；深色主题保留独立的曲线颜色变量。
- 浏览器检查覆盖标题、导航、资源内联、活动弹层、图标可见性、动态效果开关与 390px 手机布局。

- 11 项 Node 测试通过：缺失指标、离线时间、多币种月费、增量更新、坐标校验、手机标签防重叠、两小时历史窗口，以及新增的视图深链接、前进 / 后退滚动恢复、修饰键打开链接。
- JavaScript 语法检查通过。
- DOM 集成检查通过：原生订阅、`data` / `payload` 批次、只修改对应节点、保留行对象与展开详情、节点增加 / 删除、搜索、在线 / 离线即时筛选、文本转义、活动记录、管理入口。
- 延迟首个响应、503 失败和刷新恢复检查通过：加载骨架退出正确；失败时保留未知状态，不伪造 0 台节点。
- 页面、全部运行脚本、CSS、COBE 本地纹理、`/api/config`、`/api/servers` 返回 200。原生 WebSocket 收到订阅确认及连续两批 `batchUpdate`，共 23 个真实样本。

## 浏览器实测

- 浏览器标题在初始加载、接口完成、切换视图后均为 `WXT Atlas · Cloudflare Server Monitor`。
- 网络页仅显示网络视图，资源页仅显示资源视图；概览保留 KPI、地区 / 地球 / 网络摘要与节点表。点击导航不触发锚点滚动，前进 / 后退恢复对应视图。
- 375、390、800、1024、1440 像素宽度无页面横向溢出。手机地区 / 网络面板默认折叠；桌面自动展开。
- 桌面日间与手机夜间截图检查通过，状态圆点含实心中心与浅色圆环，手机流量 / 运行时间并排。
- 地区筛选、名称搜索、空结果清除筛选、详情展开、切换视图保留筛选与详情、显示设置持久化、地球关闭与恢复已检查。
- Console 未捕获应用警告或错误。真实节点数值持续变化，顶部实时连接状态正常。

## 限制

- 本次使用桌面浏览器的窄屏视口，未进行 iPhone Safari 真机验证。
- 活动仅记录当前页面会话；资源页为当前用量，无跨会话事件存储或资源长期历史。
- 地球点位默认使用地区中心；只有已有主题配置提供连接关系时才画连接线。
- 预览代理与真实数据夹具未纳入主题仓库。CDN 缓存与生产安装结果需要在发布后通过固定提交地址核对。

## v0.2.5 · 网络历史与字体（2026-09-08）

对照用户后端 `theme-develop.md` 与公开接口：8 台节点各返回 20 个 ping / loss 点，时间戳全部对应，跨度约 114 分钟；配置为两小时 20 点，约六分钟一次。因此固定五分钟状态格不能用来判断数据完整性。

本轮验证：
- 19 项 Node 测试通过，新增稀疏真实采样、独立延迟 / 丢包更新、原始时间戳、节点等权历史均值等回归。
- DOM 集成：8 台节点、24 张图同时呈现；20 个稀疏采样对应 20 个真实状态格，没有人为制造的空槽。节点新增 / 删除 / 重排、名称转义、CPU 更新复用图表、关闭历史后清空旧提示均通过。
- 保留原有详情、筛选、活动、加载失败重试及原生 WebSocket 回归；浏览器连接真实 Worker REST / WS 验证实时数据。
- 浏览器检查桌面和 390px 嵌入视口（内容区 375px，扣除滚动条）无横向溢出；手机检查详情展开、设置视图、状态格点选及深浅主题。该检查不等同于 iPhone Safari 实机测试。
- Geist 本地 WOFF2 69,760 字节，许可随文件附带；新增资源从同源加载，控制台未出现字体或脚本错误。

没有重跑线上 PageSpeed；不声称此次更改带来特定性能分数。

