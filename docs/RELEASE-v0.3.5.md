# v0.3.5

- 移除节点 Drawer 弹出层和遮罩，恢复列表行内详情。点击节点名或“详情”展开，再次点击收起。
- 节点 CPU/上下行折线及网络状态条移入折叠详情，默认不占用节点主行。首次展开才创建图表DOM，收起时继续接收真实采样但不绘制；展开后显示最新数据，保留锁定采样。
- 恢复手机右上角“管理后台”链接，窄屏允许操作区自然换行，不再隐藏入口。
- 网络页面保持独立历史图。初始9/19格是当时已收到的真实历史点数量，历史接口加载和实时采样可能改变数量，超过60点时合并为最多60格；不是固定必须达到60，窗口滚动也可能减少点数。
- 无新增运行时依赖；右上角搜索入口继续移除。页脚/缓存版本v0.3.5。

验证：npm test 28项通过；npm run check通过。Chrome 1440/390/320px验证行内开合、延迟创建图表、收起隐藏、采样选中和焦点在实时更新后保留、刷新缓存恢复、手机管理链接和无横向溢出。

将完整包内容覆盖仓库根目录即可，不运行旧版apply脚本。未修改GitHub或线上设置。
