import {coordinate,region,online,bytes,ping} from './data.js?v=0.3.2';
import {placeLabel} from './map-layout.js?v=0.3.2';
const $=s=>document.querySelector(s);
export class NodeMap{
 constructor(onRegion){this.onRegion=onRegion;this.servers=[];this.options={};this.groups=[];this.labels=new Map();this.phi=0;this.theta=.2;this.pending=0;this.signature='';this.dirty=true;this.hoverKey=null;this.active=true;this.mode='auto';this.reduced=false;this.inView=typeof IntersectionObserver==='undefined';this.readyToInit=false;this.mobile=matchMedia('(max-width:800px)').matches;
  const canvas=$('#globe');
  canvas.addEventListener('pointerdown',e=>{this.drag={x:e.clientX,y:e.clientY};canvas.setPointerCapture(e.pointerId);});
  canvas.addEventListener('pointermove',e=>{if(!this.drag)return;this.phi+=(e.clientX-this.drag.x)*.008;this.theta=Math.max(-1.2,Math.min(1.2,this.theta+(e.clientY-this.drag.y)*.005));this.drag={x:e.clientX,y:e.clientY};this.requestDraw();});
  for(const event of ['pointerup','pointercancel','lostpointercapture'])canvas.addEventListener(event,()=>{this.drag=null;});
  canvas.addEventListener('keydown',e=>{if(!['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(e.key))return;e.preventDefault();this.phi+=e.key==='ArrowRight'?.15:e.key==='ArrowLeft'?-.15:0;this.theta=Math.max(-1.2,Math.min(1.2,this.theta+(e.key==='ArrowDown'?.1:e.key==='ArrowUp'?-.1:0)));this.pauseUntil=Date.now()+2000;this.requestDraw();});
  canvas.addEventListener('webglcontextlost',e=>{e.preventDefault();this.unavailable();});
  new ResizeObserver(()=>{for(const button of this.labels.values()){button._width=0;button._height=0;}this.requestDraw();}).observe($('#map-stage'));
  if(typeof IntersectionObserver!=='undefined')new IntersectionObserver(entries=>{this.inView=entries[0].isIntersecting;if(this.inView)this.requestDraw();},{threshold:0}).observe($('#map-stage'));
  const reduced=matchMedia('(prefers-reduced-motion: reduce)');
  this.rotation=setInterval(()=>{if(this.mode!=='auto'||this.reduced||!this.active||!this.inView||document.hidden||this.drag||this.hoverKey||this.failed||!this.globe||reduced.matches||Date.now()<this.pauseUntil)return;this.phi+=.0048;this.requestDraw();},80);
  addEventListener('pagehide',()=>{clearInterval(this.rotation);this.globe?.destroy();});
  document.addEventListener('visibilitychange',()=>{if(!document.hidden)this.requestDraw();});
 }
 async init(){this.readyToInit=true;if(this.globe||this.initializing||this.failed||!this.active||!this.inView||this.mode==='off'||document.hidden)return;this.initializing=true;try{const {default:createGlobe}=await import('./vendor/cobe.js?v=0.3.2');if(!this.active||!this.inView||this.mode==='off'||document.hidden)return;const canvas=$('#globe');if(!canvas.getContext('webgl2',{alpha:true,antialias:true})&&!canvas.getContext('webgl',{alpha:true,antialias:true}))throw Error();this.globe=createGlobe(canvas,{width:$('#globe-holder').clientWidth||(this.mobile?265:355),height:$('#globe-holder').clientWidth||(this.mobile?265:355),devicePixelRatio:this.mobile?1.5:1.8,phi:this.phi,theta:this.theta,dark:0,diffuse:1.2,mapSamples:this.mobile?10000:16000,mapBrightness:6,mapBaseBrightness:0,baseColor:[1,1,1],markerColor:[.1,.65,.4],glowColor:[1,1,1],scale:1,markers:[],arcs:[],onTextureError:()=>this.unavailable()});this.draw();}catch{this.unavailable();}finally{this.initializing=false;}}
 unavailable(){this.failed=true;$('#globe-holder').hidden=true;$('#map-labels').hidden=true;$('#globe-error').hidden=false;$('#map-hint').textContent='地球暂不可用';}
 update(servers,options){this.servers=servers;this.options=options;const groups=new Map();for(const s of servers){const c=coordinate(s,options);if(!c)continue;const key=c.join(',');const group=groups.get(key)||{key,location:c,code:region(s.region).code,members:[]};group.members.push(s);groups.set(key,group);}this.groups=[...groups.values()];
  const sig=JSON.stringify([this.groups.map(g=>[g.key,g.members.map(s=>[s.id,s.name,online(s)])]),options.connections]);if(sig!==this.signature){this.signature=sig;this.dirty=true;this.requestDraw();}if(this.hoverKey)this.showTip(this.hoverKey);
 }
 focus(code){const s=this.servers.find(s=>region(s.region).code===code),c=s&&coordinate(s,this.options);if(c){this.phi=-c[1]*Math.PI/180-Math.PI/2;this.theta=c[0]*Math.PI/180;}this.pauseUntil=Date.now()+1500;this.requestDraw();}
 requestDraw(){if(this.mode==='off'||!this.active||!this.inView||this.pending||document.hidden)return;if(!this.globe){if(this.readyToInit)this.init();return;}this.pending=requestAnimationFrame(()=>{this.pending=0;this.draw();});}
 links(){const byId=new Map(this.servers.map(s=>[s.id,s]));return(Array.isArray(this.options.connections)?this.options.connections:[]).slice(0,80).flatMap(link=>{const a=byId.get(link.from),b=byId.get(link.to),from=a&&coordinate(a,this.options),to=b&&coordinate(b,this.options);return from&&to&&link.from!==link.to?[{from,to}]:[];});}
 showTip(key){const group=this.groups.find(g=>g.key===key);if(!group){this.clearTip();return;}this.hoverKey=key;const tip=$('#map-tip');tip.textContent=group.members.map(s=>`${s.name} · ${region(s.region).name} · ${online(s)?'在线':'离线'}\n联通 ${ping(s.ping_cu)}\n↓ ${online(s)?bytes(s.net_in_speed,true):'—'}  ↑ ${online(s)?bytes(s.net_out_speed,true):'—'}`).join('\n');tip.hidden=false;const ids=new Set(group.members.map(s=>s.id));document.querySelectorAll('.node-row').forEach(row=>row.classList.toggle('map-selected',ids.has(row.dataset.id)));}
 clearTip(){this.hoverKey=null;$('#map-tip').hidden=true;document.querySelectorAll('.map-selected').forEach(row=>row.classList.remove('map-selected'));}
 draw(){if(!this.active||!this.inView||this.mode==='off'||document.hidden)return;const dark=document.documentElement.dataset.theme==='dark';if(this.globe&&!this.failed){const appearance=this.dirty||dark!==this.dark?{dark:dark?1:0,baseColor:dark?[.78,.86,.9]:[1,1,1],glowColor:dark?[.102,.125,.145]:[1,1,1],mapBrightness:dark?4:6,markers:this.groups.map(g=>({location:g.location,size:.025,color:g.members.some(s=>online(s))?[.15,.72,.46]:[.65,.4,.4]})),arcs:this.links(),arcColor:dark?[.25,.8,.7]:[.1,.5,.4],arcWidth:.45,arcHeight:.18}:{};const size=$('#globe-holder').clientWidth||(this.mobile?265:355),dimensions=size!==this.renderSize?{width:size,height:size}:{};this.globe.update({phi:this.phi,theta:this.theta,...dimensions,...appearance});this.renderSize=size;this.dirty=false;this.dark=dark;}
  const stage=$('#map-stage'),width=stage.clientWidth,height=stage.clientHeight,radius=$('#globe-holder').clientWidth*.4,occupied=[],keep=new Set();
  for(const g of this.groups){keep.add(g.key);let button=this.labels.get(g.key);if(!button){button=document.createElement('button');button.className='map-label';button.innerHTML='<i class="status-dot" aria-hidden="true"></i><span></span>';button.addEventListener('mouseenter',()=>this.showTip(g.key));button.addEventListener('focus',()=>this.showTip(g.key));button.addEventListener('mouseleave',()=>this.clearTip());button.addEventListener('blur',()=>this.clearTip());button.addEventListener('click',()=>{this.onRegion(g.code);this.showTip(g.key);});$('#map-labels').append(button);this.labels.set(g.key,button);}
   const text=g.code+(g.members.length>1?' · '+g.members.length:'');if(button.lastElementChild.textContent!==text){button.lastElementChild.textContent=text;button._width=0;button._height=0;}const title=g.members.map(s=>s.name).join(' / ');if(button.title!==title){button.title=title;button.setAttribute('aria-label',`${region(g.code).name}，${g.members.length}个节点，点击筛选`);}button.classList.toggle('offline',!g.members.some(s=>online(s)));
   const a=g.location[0]*Math.PI/180,b=g.location[1]*Math.PI/180-Math.PI,p=[-Math.cos(a)*Math.cos(b),Math.sin(a),Math.cos(a)*Math.sin(b)];const rx=Math.cos(this.phi)*p[0]+Math.sin(this.phi)*p[2],ry=Math.sin(this.phi)*Math.sin(this.theta)*p[0]+Math.cos(this.theta)*p[1]-Math.cos(this.phi)*Math.sin(this.theta)*p[2],rz=-Math.sin(this.phi)*Math.cos(this.theta)*p[0]+Math.sin(this.theta)*p[1]+Math.cos(this.phi)*Math.cos(this.theta)*p[2];button.hidden=rz<=0;if(button.hidden)continue;const pos=placeLabel(width/2+rx*radius,height/2-ry*radius,button._width||(button._width=button.offsetWidth||48),button._height||(button._height=button.offsetHeight||26),width,height,occupied);button.hidden=!pos;if(pos){button.style.left=pos.x+'px';button.style.top=pos.y+'px';}
  }
  for(const [key,b]of this.labels)if(!keep.has(key)){b.remove();this.labels.delete(key);}
 }
}
